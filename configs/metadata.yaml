# General
seed: 42
device: cuda

# Data
data:
  root: /data/jakarta
  batch_size: 32
  num_workers: 4

# Model
model:
  type: gnn
  hidden_dim: 128
  num_layers: 4
  dropout: 0.2

# Training
training:
  lr: 0.001
  epochs: 100
  weight_decay: 1e-4